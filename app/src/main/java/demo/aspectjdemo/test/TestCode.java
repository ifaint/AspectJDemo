package demo.aspectjdemo.test;

import android.util.Range;

import java.awt.font.NumericShaper;

/**
 * Created by zhangxiaofei on 2017/3/10.
 */

public class TestCode {
/*
    private int[][] sample1 = {{1,3}, {3,5}, {1,4}, {6,10}, {10,11}};
    int[][] merge(int[][] input){
        if(input.length<2)
            return input;
//        int begin = input[0][0];
//        int end = input[0][1];
        Range range = new Range(input[0][0],input[0][1]);
        for(int i=1;i<input.length;i++){
            int begin = input[i][0];
            int end = input[i][1];
            if(range.intersect(begin,end) !=null)
                range = range.extend(begin,end);
        }
        return range;
    }

*/

}
